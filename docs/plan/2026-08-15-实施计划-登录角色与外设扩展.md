# 实施计划：操作者登录/角色体系 + 外设测试扩展

| | |
|---|---|
| 日期 | 2026-08-15 |
| 状态 | P1/P2 本批完成，其余按批推进 |
| 依据 | 用户需求（登录三级角色沿用 `D:\CP3_pro_test\Application` 老产测惯例，只参考设计不搬代码）；外设扩展依据评审表 §1.3（V0.2） |

## 0. 身份分层（先厘清，两套账号互不相干）

- **操作者身份**（本计划的登录）：工号+密码，本地 SQLite，**完全离线**（产线内网）。决定 UI 权限与审计归属。
- **软件级云凭证**（腾达后台账号，即 PC 版 App 那个登录）：软件连云用，由超级用户在设置页配置一次、本地加密存储（DPAPI），与操作者无关。

## 1. 角色与权限矩阵（唯一权威表，实现时以此为准）

| 能力 | 技术员 | 工程师 | 超级用户 |
|---|---|---|---|
| 调焦/准成品/成品/检查工位标准流程 | ✓ | ✓ | ✓ |
| 单项重测 | ✓ | ✓ | ✓ |
| 跳过测试项（强制填原因入台账） | ✗ | ✓ | ✓ |
| 调试面板（手动单发指令/原始流水） | ✗ | ✓ | ✓ |
| 台账导出 | ✗ | ✓ | ✓ |
| 创建/重置技术员账号 | ✗ | ✓ | ✓ |
| 创建/重置工程师账号 | ✗ | ✗ | ✓ |
| 产品 profile 编辑（测试项集合/期望版本/阈值） | ✗ | ✗ | ✓ |
| 后台 API 凭证配置 | ✗ | ✗ | ✓ |

UI 惯例（沿用老软件）：无权限控件对技术员**直接隐藏**而非置灰（SW_HIDE，看不见就不会问）；登录框记住上次工号；**换班切换用户不退软件、不清产品会话与设备连接**；标题栏常显「产品 · 工号(角色)」。

安全决策（升级两处，不照搬老实现）：密码 **PBKDF2-HMAC-SHA256 加盐哈希**（Qt `QPasswordDigestor`，迭代 ≥10 万次；老软件 MD5/Base64 弃用）；**废除硬编码万能密码**（老 super_password=tenda_mfg 写死代码里）——超级用户忘密兜底 = 删除本地账户库文件回到首启建号流程（**产测台账独立存放不受影响**，写入管理员说明）；登录失败简单节流（连错 5 次锁 30 秒）。

超级用户唯一内置：首次启动强制创建并设密码，不可新建第二个、不可删除。

审计（P6）：指令流水与产测台账全部记操作者工号+角色；**维修清除/跳过测试项/profile 修改/账号操作**四类记独立审计日志（时间/工号/设备 SN/参数/原因）。

## 2. 批次索引（明细见各分册）

| 批次 | 内容 | 状态 | 分册 |
|---|---|---|---|
| P1+P2 | 登录门 + 权限矩阵落 UI | ✅ 已完成 | [P1-P2](P1-P2-登录门与权限矩阵.md) |
| P3 | 账号管理页 + 首启建号 | 待做（纯 UI） | [P3](P3-账号管理与首启建号.md) |
| P4 | 设置页：云凭证 + profile 编辑 | 待做（纯 UI） | [P4](P4-设置页与profile编辑.md) |
| P5 | C++ 后端：SQLite/PBKDF2/节流/忘密兜底 | 待做（真实实现期） | [P5-P6](P5-P6-C++后端与审计.md) |
| P6 | 审计：流水带工号 + 四类审计日志 | 待做（真实实现期） | [P5-P6](P5-P6-C++后端与审计.md) |
| P7 | 调试面板 + 台账导出（工程师+） | 待做 | 随 P4 一并设计 |
| P8 | 4G 双 SIM 逐槽（串行 + 90s 超时） | 待做 | [P8-P11](P8-P11-外设测试扩展.md) |
| P9 | SD 读写校验（1MB verify） | 待做 | [P8-P11](P8-P11-外设测试扩展.md) |
| P10 | 纯 PC 三条：版本校验/外电/喇叭咪头回环 | 待做 | [P8-P11](P8-P11-外设测试扩展.md) |
| P11 | profile schema v2（承载 P8-P10 配置） | 待做 | [P8-P11](P8-P11-外设测试扩展.md) |

### 依赖与顺序

P1→P2 已完成。P3/P4 纯 UI，随时可开工。P5/P6 与 C++ 后端框架、协议接入同期落地
（需先定落盘目录，见开放问题 2）。P8–P11 的 UI/编排可先做 mock，真实生效等协议接通。

**任何批次都不阻塞播放 spike 与协议接入** —— 那两个卡在外部前置（三方库缺失、
云端建模），与本计划并行。

## 3. 已拍板决策（2026-08-15）

1. **落盘位置**：账户库与产测台账都放**程序同目录**（绿色版，随 dist 走）。
   ⚠️ 由此产生两条约束：①**不要装进 `Program Files`** —— 那里默认不可写，写失败会被
   UAC 虚拟化到 `%LOCALAPPDATA%\VirtualStore`，产线上极难排查；装到 `D:\产测工具\`
   这类可写目录，或做成免安装绿色版（当前 dist 就是）。②账户库与台账**仍要分成两个
   文件**（`accounts.db` / `ledger.db`），因为忘密兜底要能删账户库而不动台账。
2. **工号不做格式校验** —— 任意字符串即可（产线工号规则可能变，校验反而添堵）。
   但仍**必须按字符串存**（`0045009` 前导零不能丢）。
3. **维修工位全角色可见** —— 技术员也能进。清除操作本身已有三重保护：
   必须先存档、二次确认、审计留痕。
4. 节流参数暂定 5 次 / 30 秒，密码复杂度暂定长度 ≥8 含字母数字；产线 IT 若有要求再调。
