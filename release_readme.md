# 产测软件 · 发版与工厂部署手册

给两种人看：**发版的人**（开发/管理员，就是你）和**工厂 IT/产线管理员**。
与根目录 `README.md` 的区别：那份讲怎么改代码、怎么构建、踩过哪些坑；这份只讲
**怎么把软件交到工厂、工厂怎么用起来、以后怎么升级** —— 不涉及任何代码。

---

## 一、角色与链路（先看懂这张图）

开发和工厂**不在同一个内网**（不同城市），所以每个版本靠人传一次，再由工厂内网分发：

```
开发机（你）                    工厂工程师                  工位 PC × N
─────────────                 ─────────────              ─────────────
① 改版本号 → build.sh         ④ 解压覆盖到厂内共享目录     首装：从共享把 files\
② publish_update.cmd 发布       （manifest.json 与 files\        整个拷到本地，双击 exe
③ 压成一个 zip 发给工程师          直接在共享根目录下）      升级：软件「关于」页点
  （微信/网盘，一版一次）                                    「检查更新」→ 只拉差异
```

**只有一种交付物**：那个 zip。里面 `files\` 就是一套完整可运行的软件目录
（实测与开发机 dist 的 1553 个文件逐一对齐），所以：

- **首次装机** = 从共享把 `files\` 拷到本地任意目录（资源管理器复制即可，不用解压
  任何东西），双击 `ProductTestTool.exe`
- **后续升级** = 软件里点「检查更新」，只下载变化的文件

不再有"软件 zip / 升级 zip"两种包 —— 一份通吃。

几条要点：

- **所有命令都在开发机执行**。工厂没有任何人跑任何命令，工程师只做"解压覆盖"。
- **服务器就是个哑共享文件夹**：不装 git、不装任何软件、不跑任何东西。
- **工位 PC 什么都不用装**：软件自己读共享、比对差异、下载、换文件、重启。
- 跨城市传输一个版本只发生**一次**；厂内 N 台 PC 各自从本地共享拉差异，
  几十 MB、秒级 —— 这就是"不用每台都下载解压一遍"。
- `files\` 里带了 `factory_config.json`（升级源已预填），但它**不在 manifest 里** ——
  首装能带走，升级永不覆盖。工厂改过的配置不会被任何一次升级动到。

---

## 二、你怎么发版（开发机上）

首装和升级用**同一个包**，所以发版流程只有一套。全部在你的开发机上做：

### 发一个版本（四步）

1. **确认升级源已填对**。`resources/factory_config.json` 的 `updateSource` 要是
   **工厂那台服务器**的共享路径（正斜杠写法）。这一项会随包发到工厂，填错工位
   PC 就检查不到更新：

   ```json
   "updateSource": "//192.168.1.10/ptest_update",
   ```

   ⚠️ 当前是**占位示例** —— 工厂还没给真实路径。拿到后改这一行，再发版。

2. **版本号 +1**：`CMakeLists.txt` 的 `project(... VERSION x.y.z)`。
   版本号不变的话，工位 PC 会认为"已是最新"，什么都不会下载。

3. **构建 + 发布**（PowerShell）：

   ```powershell
   ./build.sh                                      # Git Bash
   .\tools\publish_update.cmd D:\ptest_publish     # 每次都发到同一个本地目录
   ```

4. **压包发给工厂工程师**（微信/网盘随意）：

   ```powershell
   Compress-Archive -Force D:\ptest_publish\* D:\ptest_v0.2.0.zip
   ```

完事。工程师解压覆盖到厂内共享后：老机器点「检查更新」拿差异，新机器直接从
共享拷 `files\`。

### 发布脚本做了什么

算 `dist\` 里每个文件的 SHA-256 → 生成 `manifest.json`（版本 + 文件清单）→
把文件树拷到目标的 `files\` 下 → 额外把 `factory_config.json` 模板和
`manifest.json` 也放进 `files\`（**不进清单**，所以首装能带走、升级不覆盖）。

共享目录里放的是**这个展开的结构**，不是 zip —— zip 工位软件读不了，也没法差分。

### 本地先验整条链

把 `dist\factory_config.json` 的 `updateSource` 指向自己的共享，publish 到那里，
软件里点检查更新：

```powershell
.\tools\publish_update.cmd \\192.168.170.201\public\ptest_update
```

⚠️ 发布脚本自动**排除**这些文件，它们永远不会被下发、也不会被升级覆盖：
`cloud_config.json`（云密钥）、`accounts.json`（账号授权）、
`factory_config.json`（工厂配置）、`station_progress.json`（工位进度）、
`profiles.json`（产品型号表）、`logs\`。

---

## 三、你给工厂什么

| 时机 | 交付物 | 工程师拿到后做什么 |
|---|---|---|
| **每个版本**（含第一版） | **一个 zip**（解压后顶层是 `manifest.json` + `files\`） | 解压覆盖到厂内共享目录，就这一步 |
| 仅第一次 | 本手册（至少第四、五节） | 照做 |
| 仅第一次 | 管理员手机号（内置超管 188****0537、工程师 188****2014） | 首次登录后在软件里加产线人员 |

首装与升级用同一个包，没有"两种 zip"。工程师永远只做"解压覆盖到共享"这一个动作。

**不要给的东西**：`cloud_config.json` 里的腾讯云密钥若已填，发包前确认 zip 里那份
是不是该给工厂 —— 密钥文件不进 git，但**会**在 dist 里随 zip 走。给工厂的包里
密钥应该是工厂自己的（或留空走 Mock，看工厂要不要跑真产测指令）。

---

## 四、工厂 IT 怎么做（一次性，约 10 分钟）

### 1. 开一个共享文件夹

在任意一台常开的 Windows/Linux(Samba) 机器上开个共享，要求只有两条：

- **工位 PC 无需输密码就能读**（everyone 只读即可）
- 工厂工程师自己**能写**（收到升级 zip 后往里解压）

例：`\\192.168.1.10\ptest_update`。以后每次收到升级 zip，解压覆盖到这里，
保证 `manifest.json` 和 `files\` 直接在该目录下（别多套一层）。

### 2. 每收到一个版本：解压覆盖到共享

把开发侧发来的 zip 解压覆盖进第 1 步那个共享目录，保证解压后是这样：

```
\\192.168.1.10\ptest_update\
├── manifest.json
└── files\            ← 一套完整的软件（含 exe）
```

别多套一层文件夹（`ptest_update\ptest_v0.2.0\files\` 是错的）。就这一个动作。

### 3. 装工位 PC：从共享把 files\ 拷到本地

资源管理器打开共享，把 **`files\` 整个文件夹**复制到工位 PC 任意目录
（建议 `D:\ProductTestTool\`），双击里面的 `ProductTestTool.exe`。

不用解压、没有安装程序、不写注册表（除"记住手机号"一项）、删目录即卸载。

升级源已经预填在 `factory_config.json` 里，**正常情况不用改**。若共享路径与
预填的不一致（换了服务器/共享名），记事本改这一行，**用正斜杠**：

```json
"updateSource": "//192.168.1.10/ptest_update",
```

改过的配置以后升级**不会**被覆盖。若要改：装第一台时改好留一份，之后每台拷进去。

填错/没填的表现：关于页点「检查更新」会明确提示（"未配置升级源"或"读不到
manifest.json"），不会悄悄失败。

### 4. 登录与账号

- 登录用**腾达安防云账号**（手机号 + 密码，就是腾达安防 App 那个）。
  没有账号的先用手机 App 注册。
- 首次由内置管理员登录 → 产品选择页底部「账号管理」→ 把产线人员的手机号
  加进去（选角色：技术员/工程师）。**没被添加的人即使有腾达账号也登不进**。
- 技术员可以限定只能操作某几个型号（默认全部）。
- 忘记密码/改密码：在腾达安防 App 里操作，软件不存任何密码。

---

## 五、工厂日常怎么升级

任意工位 PC：**关于页 → 检查更新**。

| 显示 | 含义 / 动作 |
|---|---|
| 已是最新 | 没新版本，完事 |
| 可升级到 vX（N 个文件，M MB） | 点「下载更新」，后台拉差异文件并逐个校验 |
| vX 已就绪 | 点「安装并重启」→ 软件退出，黑色窗口闪过（正在换文件），几秒后新版自动启动 |
| 出错（红字） | 按提示处理，多为共享目录连不上；重试即可，下载坏了会自动挡住不安装 |

- 每台 PC 各自升级，互不影响；建议**换批次间隙**升，别在测到一半时点
- 升级**不会**动工厂配置/账号/进度/密钥 —— 这些文件在排除清单里
- 升级失败不会破坏现有安装：文件先下到暂存目录、校验通过才替换

---

## 六、常见问题

**Q：服务器要装 git / 装服务吗？publish_update.cmd 在哪跑？**
服务器什么都不装 —— 它只是个文件夹。`publish_update.cmd` **永远在开发机上跑**；
工厂侧唯一的动作是工程师把升级 zip 解压覆盖到共享目录。

**Q：跨城市每次都要传 358MB 吗？**
zip 目前是全量（压缩后约 250MB），但只传**一次**（给工程师），厂内 N 台 PC 各自
只拉差异。若嫌传给工程师这一步也大，可以让开发侧出"差量 zip"（只含变化的文件 +
新 manifest，解压覆盖后共享目录照样完整）—— 需要时找开发加，机制天然支持。

**Q：共享上的 files\ 会不会越升越旧？**
不会。每次覆盖解压都会把 `files\` 更新成最新版，所以从共享拷出来装的新机器
永远是最新的。

**Q：能用 http 地址吗？**
现在不能，只支持共享目录（UNC 路径）。要 http 源另提需求。

**Q：升级包被人篡改怎么办？**
当前只做了完整性校验（每文件 SHA-256，防传输损坏），**没有防篡改签名** ——
内网可控场景够用；若共享目录不可控，找开发补验签再上。

**Q：新工位 PC 怎么装？**
从共享把 `files\` 拷到本地即可（就是共享上那份，永远是最新版）。不用找开发要包。

**Q：把软件目录整个删了会丢什么？**
账号授权表、工厂配置、工位进度、云密钥 —— 都在软件目录里。删之前把
`accounts.json` / `factory_config.json` / `cloud_config.json` 拷走即可原样恢复。
